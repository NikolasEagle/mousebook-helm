apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mousebook.fullname" . }}-test-connection"
  labels:
    {{- include "mousebook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mousebook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
